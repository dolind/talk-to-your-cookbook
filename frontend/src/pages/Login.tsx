import AuthForm from '../components/AuthForm';

const Login = () => {
    return (
        <div>
            <h1>Login</h1>
            <AuthForm mode="login"/>
        </div>
    );
};

export default Login;
